export default function EditBanner() {
  return (
    <div className="edit-banner">
      <span className="warn">⚠</span>
      <span>
        Edit mode is on — renaming, deleting, and reordering are available. Pages
        can&apos;t be deleted while they have sub-pages.
      </span>
    </div>
  );
}
